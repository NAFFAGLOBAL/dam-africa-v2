import { Router } from 'express';
const router = Router();

// Placeholder - Will be implemented
router.get('/', (req, res) => {
  res.json({ message: 'Payments module - Coming soon' });
});

export default router;
