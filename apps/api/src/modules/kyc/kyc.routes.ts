import { Router } from 'express';
const router = Router();

// Placeholder - Will be implemented
router.get('/', (req, res) => {
  res.json({ message: 'KYC module - Coming soon' });
});

export default router;
